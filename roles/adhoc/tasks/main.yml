--- 
- 
  ios_command: 
    commands: "{{ item }}"
    provider: "{{ provider }}"
    wait_for: result[0] contains IOS
  name: "Run adhoc commands"
  with_items: "{{ commands }}"
  register: output
  
- name: display output
  debug: 
    var: output
